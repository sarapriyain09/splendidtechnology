# Prompts

Domain prompt templates should be stored here instead of hardcoding prompts in agent classes.

Suggested subfolders:
- `crm/`
- `sales/`
- `marketing/`
- `media/`
- `support/`

Each prompt should support explicit versioning and safe fallback behavior.
