# Workflows

Reusable multi-step flows that agents can execute.

Examples:

- lead qualification flow
- follow-up cadence flow
- campaign draft and publish flow
- invoice follow-up flow

Each workflow should be versioned and observable.
